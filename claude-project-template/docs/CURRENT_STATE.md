# <ProjectName> — Current State

**Last updated:** <YYYY-MM-DD> (project initialized from template)

> This file carries only the **NEXT ACTION** + this-session deltas. It does **NOT** keep its own copy of the phase/block status list — that lives in the "status at a glance" spine in `ROADMAP.md` (the single source of truth). See `PROTOCOL.md` → "Where-are-we: one source of truth, frozen numbers".

---

## 📍 NEXT ACTION

<TODO: ONE unambiguous line — the single next thing to do, matching the spine's CURRENT phase/block. Session-start reports this verbatim.>

Phase 0 — project scaffold. <TODO: customize once the roadmap + spine land.>

**Build status:** **Not yet tested.** Fresh checkout, no code written.

**Remote:** <TODO: github URL once set up>. `main` matches origin.

---

## Optional loose ends (NOT the next step)

<TODO: minor leftovers, clearly labeled as NOT the priority so they can't be mistaken for the NEXT ACTION.>

---

## Workflow status

Sessions run in `<project-dir>` on `main`. Worktrees forbidden (see `.claude/commands/start.md` Step 0 for why).

**Canonical dev commands:**
- <TODO: fill in as the project scaffolds>

---

## What happened this session

<TODO: overwritten by `/end` each session. This is the Day-0 placeholder.>

---

## Active blockers

None.

---

## Notes & things to watch

<TODO: empty on Day 0. Accumulates cross-session reminders as the project evolves.>
