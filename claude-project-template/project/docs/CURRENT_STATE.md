# <ProjectName> — Current State

**Current phase:** <Phase/Block name> — <one-line status>
**Build status:** not yet tested

_Last updated: <YYYY-MM-DD HH:MM> (<track, if any>)_

> This file carries only the **NEXT ACTION** + this-session deltas. It does **NOT** keep its own copy of the phase/block status list — that lives in the "status at a glance" spine in `ROADMAP.md` (the single source of truth). Overwrite at `/end` after re-reading from disk; never append.

---

## 📍 NEXT ACTION

<ONE unambiguous line — the single next thing to do, matching the spine's CURRENT phase/block. Session start reports this verbatim.>

<!-- MULTI-TRACK REPOS: one section per track, plus a Shared block. Each session rewrites only its own section and the shared facts it changed; the other track's section is copied through verbatim.

## 📍 NEXT ACTION — desktop
<one line>

## 📍 NEXT ACTION — mobile
<one line>

## Shared
- Server deployed: v<N> (by <track>, <date>) — <what it carries>
- Schema / migration: <NNNN> applied to prod (by <track>, <date>)
- <anything both tracks consume>
-->

## Build status

<working / broken / not yet tested — what was verified, on what, when. Per track in multi-track repos.>

## What the last session did

<numbered, short. Your track only in multi-track repos.>

## Loose ends (NOT the next step)

<Point at open ledger IDs: "open: L-3, L-8 — see docs/SESSION_LEDGER.md". No separate prose list here; regenerated prose silently drops items.>

## Active blockers

None.

## Things to watch

<cross-session reminders. In multi-track repos: the other track's files left in the tree at this wrap, and any of their commits your push carried.>
